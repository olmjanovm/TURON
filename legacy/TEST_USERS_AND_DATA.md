# Testing Actors & Seed Data Requirements

To comprehensively evaluate all execution boundaries cleanly, you should construct the following Test Nodes globally natively physically.

## 1. Actor Telegram Accounts
You should leverage physical Telegram testing accounts for seamless validation:
* **Admin Node (`account A`)**: Native Administrator targeting `/admin_stats` and executing `Broadcasts` tracking limits successfully locally natively.
* **Courier Node (`account B`)**: Native driver mapping intercepts safely verifying GPS Google bounds properly locally natively.
* **Customer Nodes (`account C`, `account D`)**: Standard testing interfaces running Checkout constraints tracking limitations natively perfectly safely locally natively.

## 2. Seed Data Scope
Leverage `npm run prisma:test-seed` internally effectively bootstrapping parameters:
* **Promos**: Deploy `DEMO20` internally handling bounds natively cleanly dynamically securely.
* **Menus**: Generate isolated `Test Categories` deploying dummy items testing string boundaries structurally cleanly dynamically.
* **Orders**: Natively simulate `1x Pending Card Transfer` to test Payment Rejection loops cleanly dynamically natively. Simulate `1x Canceled Order` safely executing stock roll-back natively efficiently cleanly!
