/**
 * Prefix used for category selection callbacks.
 * @constant {string}
 */
const CATEGORY_PREFIX = 'category_';

/**
 * Prefix used for product selection callbacks.
 * @constant {string}
 */
const PRODUCT_PREFIX = 'product_';

/**
 * Callback data string used to return to the categories inline menu.
 * @constant {string}
 */
const BACK_TO_CATEGORIES_CALLBACK = 'back_categories';

module.exports = {
  CATEGORY_PREFIX,
  PRODUCT_PREFIX,
  BACK_TO_CATEGORIES_CALLBACK
};
